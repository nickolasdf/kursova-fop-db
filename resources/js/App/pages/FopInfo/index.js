import React from "react";
import { useParams } from "react-router";
import "./index.scss";

const FopInfo = () => {
    const { id } = useParams();
    return (
        <div>

        </div>
    )
};