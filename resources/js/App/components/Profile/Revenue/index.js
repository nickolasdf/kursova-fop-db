import React from "react";
import "./style.scss";

const Revenue = props => {
    return (
        <div
            className={`revenue__container ${props.styleName}`}
        >
            Revenue
        </div>
    );
};

export default Revenue;
