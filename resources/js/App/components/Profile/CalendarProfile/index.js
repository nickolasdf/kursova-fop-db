import React from "react";
import "./style.scss";

const CalendarProfile = props => {
    return (
        <div
            className={`calendar-profile__container ${props.styleName}`}
        >
            CalendarProfile
        </div>
    );
};

export default CalendarProfile;
