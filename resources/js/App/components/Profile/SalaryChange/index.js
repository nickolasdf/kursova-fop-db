import React from "react";
import "./style.scss";

const SalaryChange = props => {
    return (
        <div
            className={`salary-change__container ${props.styleName}`}
        >
            SalaryChange
        </div>
    );
};

export default SalaryChange;
