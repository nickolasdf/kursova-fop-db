import React from "react";
import "./style.scss";

const VacationDuration = props => {
    return (
        <div
            className={`vacation-duration__container ${props.styleName}`}
        >
            VacationDuration
        </div>
    );
};

export default VacationDuration;
